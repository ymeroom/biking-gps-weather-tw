# 交班單 — 2026-09-06

## 現況：v1c 已上線且驗證過

- **App**：https://ymeroom.github.io/biking-gps-weather-tw/（GitHub Pages，`main` 分支的 `index.html`）
- **Worker**：`rainbow-proxy.ymeroom.workers.dev`（藏 Rainbow 金鑰、補 CORS、Origin/Referer 白名單）
- **雷達來源鏈**：Rainbow 未來 2h 預報 → CWA 觀測（`data` 孤兒分支）→ RainViewer
- **降雨面板**：Rainbow 點位 nowcast「此處約 X 分後開始下…」＋ CWA 縣市 3h 機率（第二行）

三份說明文件都是最新的，優先看這些：
- `README.md` — 架構、資料源、部署/維護
- `worker/README.md` — Worker 路由、部署、換金鑰
- `plans/2026-09-06-單車GPS降水地圖-台灣版-v1c-Rainbow.md` — v1c 完整計劃＋兩輪 advisor 修正紀錄
- `docs/guide.html` — 給使用者的操作手冊（也發成 artifact）

## 金鑰 / secret（都不在 repo）

| 名稱 | 存在哪 | 怎麼換 |
|---|---|---|
| `RAINBOW_KEY` | Cloudflare Worker secret | `cd worker && wrangler secret put RAINBOW_KEY`（**這台機器互動貼上會截斷，要一個字一個字打或用 `printf %s '金鑰' \| wrangler secret put RAINBOW_KEY`**） |
| `CWA_KEY` | GitHub repo Actions secret | `gh secret set CWA_KEY` |

- 舊的 Rainbow 金鑰（`5b68…`）已在 portal 撤銷，別再用。
- `wrangler` 已登入，token 有快取，`wrangler deploy` 直接可用。

## 還沒驗、下次要處理

1. **真機測**（只有騎出去才測得到）：
   - `coords.heading` / `speed` 在實際騎乘速度下會不會回值 → 決定橘色推算點出不出現（App 已有「手機還沒回報方向」提示 fallback）
   - 4G / 山區弱訊號下播放的實感
   - 面板在真手機佔多少螢幕（桌機模擬 360px 時 23–26%）
2. **Rainbow 圖磚色階校準**：`index.html` 的 `RAINBOW_SCALE` 是取樣估的（藍→靛→紫）。遇到大雨對一下實際顏色再修。
3. **CWA cron**：`fetch-cwa.yml` 排程從 `*/12` 改成 `*/15` 了，但之前觀察到它幾乎不自己觸發（都要 `gh workflow run`）。它現在只是後備 + 縣市預報來源，優先度低。若縣市面板長期空白 → 檢查這個。
4. **Rainbow 用量**：免費額度圖磚 30k/nowcast 5k 每月，**超量計費**。第一次實地騎乘後看 rainbow.ai dashboard，用量偏高再考慮在 Worker 加每日硬上限。

## 開新 session 的注意事項

- 用 `D:\biking-gps-weather-tw` 當根目錄：**程式沒問題**，repo 自足。
- 但 auto-memory 綁在 `D:\2026東京單車騎旅` 那個專案路徑下，換根目錄就不會自動載入。
  相關記憶：`biking-gps-weather-tw-project.md`、`rainbow-weather-api.md`、`cwa-radar-data.md`、`jma-radar-tile-api.md`
  （都在 `C:\Users\ymero\.claude\projects\D--2026------\memory\`）。
  需要那些背景時，要嘛從 `D:\2026東京單車騎旅` 開 session、要嘛請新 session 去讀那個路徑。
- 日本版 `D:\biking gps weather`（`ymeroom/biking-gps-weather`）是獨立 repo，跟這個不共用程式。
